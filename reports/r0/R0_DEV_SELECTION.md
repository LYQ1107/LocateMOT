# R0 dev selection

Status: `NOT_RUN_BY_DATA_CONTRACT_STOP`.

No dense dev scoring or checkpoint shortlist was produced. The registered
selection rule therefore selected no checkpoint. Fixed/internal semantic
labels were not used for selection.
