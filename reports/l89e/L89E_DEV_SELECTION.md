# L89E dev selection

The merged fit/dev pool contains 20 epochs and 9960 rows.
The registered shortlist contains 5 candidates
with epochs `[8, 20, 40, 32, 4]`.  The true-native-timeline dev
matrix contains 15 complete TrackEval results
(each candidate under B/R/P).

The preregistered tuple
`(HOTA, DetA, AssA, distinct_target_recall, -inactive_false_acceptance,
-epoch, deterministic_rule_order)` selected epoch `4`,
phase `S`, Rule `B`.  Selection was frozen before
fixed validation and no fixed calibration/validation labels were used here.

Phase policy: `{"epoch": 4, "history_length": 0, "history_mode": "zero_history", "phase": "S", "temporal_enabled": false}`.
